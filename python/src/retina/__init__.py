"""retina — the experimental webcam-as-ambient-light-sensor path (parked).

Camera capture, scene-brightness estimation, and the virtual HID ALS (uhid) that
lets GNOME read the webcam as a sensor. Superseded for shipping by the phone/BLE
path (pupil -> iris); kept for provenance and possible revival. See docs/retina/.
"""
